<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest template for DevHQ.
  Tokens (e.g. {{VERSION}}) are substituted by scripts/package-msix.ps1.

  Identity values below MUST match what you reserved in Partner Center
  (Product identity page). Pass them to the script via parameters, or set
  the defaults there. Do NOT guess them - a mismatch is rejected at submit.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION}}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>{{DISPLAY_NAME}}</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>{{DESCRIPTION}}</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="App" Executable="DevHQ.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="{{DISPLAY_NAME}}"
        Description="{{DESCRIPTION}}"
        BackgroundColor="#0c0d11"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Square71x71Logo="Assets\Square71x71Logo.png" Square310x310Logo="Assets\Square310x310Logo.png" Wide310x150Logo="Assets\Wide310x150Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <!-- The webview pulls its fonts from Google Fonts, and `git` reaches the
         network on the user's behalf. -->
    <Capability Name="internetClient" />
    <!-- DevHQ is a desktop app in a package: it reads folders the user points
         it at, shells out to `git`, walks the process table and starts real
         pseudoconsoles. None of that is possible without full trust. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
